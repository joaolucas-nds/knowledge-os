# Copie para .env e ajuste com as credenciais reais (nunca commitar o .env).
# Usado por drizzle-kit (generate/migrate/studio) e por apps/api.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/knowledge_os"
