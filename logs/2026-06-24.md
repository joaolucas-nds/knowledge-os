# Log — 2026-06-24

## Objetivo da sessão
Retomar e implementar de fato a F0 (Fundação Técnica) do Knowledge OS: monorepo, schema de dados, API e frontend mínimos, validados de ponta a ponta — não só projetados.

## Feito

### Monorepo
- Criado monorepo real com Turborepo + pnpm workspaces: `apps/web`, `apps/api`, `packages/db`, `packages/crdt`, `packages/ui` (ver DEC-008).
- TypeScript strict configurado em todos os workspaces (`tsconfig.base.json` herdado pelos packages Node; `apps/web` com config própria por exigência do Next.js).
- ESLint flat config em todos os workspaces — `apps/web` em ESLint ^9 (compat. com `eslint-config-next`), demais em ESLint ^10 (ver DEC-010).
- Todas as versões de dependências foram resolvidas contra o npm registry real via `pnpm add` (não chutadas de memória) — isso revelou que Next.js 15 (documentado no CONTEXT original) já estava obsoleto; Next.js 16 é a major estável atual (ver DEC-009).

### packages/db
- Schema Drizzle inicial: `blocks` + `relations` (escopo estrito da F0, conforme ROADMAP — `canvas_nodes`/`canvas_edges`/`database_schemas`/`search_vectors` ficam para F2/F3/F6).
- Migration gerada (`drizzle-kit generate`) e **aplicada contra um Postgres 16 real** instalado no sandbox (`drizzle-kit migrate`).
- Smoke test (insert + select via Drizzle) executado e validado contra esse Postgres real.

### apps/api
- Fastify 5 com `/health` e `/health/db`, CORS e rate limit registrados.
- **Bug real encontrado e corrigido em produção de código (não só revisão):** `/health/db` reportava "not_configured" mesmo com `DATABASE_URL` definido em `.env` — `loadEnv()` nunca carregava o arquivo. Corrigido com a flag nativa do Node 22 `--env-file-if-exists` nos scripts `dev`/`start`. Validado batendo no endpoint antes (erro) e depois (ok) da correção. Registrado como FIX-001.

### apps/web
- Next.js 16 (Turbopack) com home page (Server Component) que faz fetch ao vivo em `/health` da API, sem cache (`cache: "no-store"`) — alinhado ao modelo de Cache Components opt-in do Next 16.
- `next build` de produção executado com sucesso. Dev server subido de verdade e testado contra a API real rodando — renderizou "✓ apps/api respondeu" com o JSON de health.

### packages/crdt e packages/ui
- Esqueletos intencionais, claramente comentados como tal. Dependências reais resolvidas (`@automerge/automerge` ^3.x — nota: DEC-004 original referenciava "2.0", revisitar na F5; `react` ^19).

### CI
- `.github/workflows/ci.yml` criado (typecheck + lint via pnpm+turbo). Não exercitado por um push real ainda.

### Validação geral
- `pnpm typecheck` e `pnpm lint` passam limpo nos 5 workspaces (zero erros, zero warnings).
- Build de produção testado em `apps/api` (tsc → dist/) e `apps/web` (`next build`).

## Decisões
- DEC-008: pnpm como gerenciador de pacotes do monorepo.
- DEC-009: Next.js 16 substitui a referência a "15" no CONTEXT original.
- DEC-010: ESLint ^9 em `apps/web`, ^10 nos demais workspaces (conflito de peer dependency com `eslint-config-next`).

## Bugs
- FIX-001: `apps/api` não carregava `.env` — corrigido com `--env-file-if-exists` do Node 22.

## Aprendizados / armadilhas
- Armadilha registrada no CONTEXT (#7): pnpm 10+ bloqueia build scripts nativos por padrão (`ERR_PNPM_IGNORED_BUILDS`) — resolver uma vez com `pnpm approve-builds --all`, persiste em `pnpm-workspace.yaml` (`allowBuilds:`).
- Armadilha registrada no CONTEXT (#8): nem todo o monorepo pode usar a mesma versão do ESLint — `eslint-config-next` trava em `^9` enquanto o resto do ecossistema já está em `^10`.
- Nota operacional (não é armadilha do projeto, é de processo desta sessão): ao validar serviços em background no sandbox, processos iniciados com `&` simples morrem quando a sessão do shell encerra entre chamadas de ferramenta — é preciso `setsid ... < /dev/null > log 2>&1 &` para sobreviver entre chamadas. Vale lembrar em sessões futuras de validação ao vivo.
- Resolver dependências contra o registry real (em vez de confiar na memória) pegou uma desatualização real e relevante na documentação (Next 15→16) antes que ela se tornasse dívida técnica.

## Onde parei
F0 parcialmente concluída e validada: monorepo, schema de dados (Postgres), API e frontend mínimos todos funcionando de verdade, não só no papel. Faltam dois itens para fechar a F0 por completo: wa-sqlite+OPFS no browser (persistência local-first) e autenticação (Better Auth vs. Clerk — decisão ainda não tomada).

## Próximos passos
- [ ] Implementar wa-sqlite + OPFS em `apps/web` (Worker, decidir VFS — provavelmente `AccessHandlePoolVFS` para permitir WAL mode, conforme DEC-003).
- [ ] Decidir Better Auth vs. Clerk e implementar login/logout.
- [ ] Trocar o card de health-check da home page por uma primeira tela real de criação de Block.
- [ ] Exercitar o CI num push real ao GitHub.
